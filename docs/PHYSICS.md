# Физика тела: песочница и разбор PEAK

Документ отвечает на два вопроса: **где экспериментировать с физикой, не трогая карту**, и **можно ли повторить телесную механику PEAK**.

Короткий ответ на второй: да, повторимо — потому что в PEAK нет того сложного, что ей приписывают. Это не активный рэгдолл, и модель там не сложнее нашей. Подробности ниже.

**Порядок работ: сначала ноги.** Руки и лазанье в песочнице выключены (F8 включает черновик). Пока человек не ходит, не берёт ступени, не буксует на крутизне и не падает как надо — лазанье настраивать не по чему.

## 1. Песочница

Отдельная сцена `Assets/Scenes/Sandbox.unity`. В ней нет ни мира, ни сети, ни ночи, ни Менка: только тело, полигон из примитивов и экран игры. Собирается за секунды, потому что не тащит рельеф (95 МБ данных мира в сборку не попадают).

### Экран

На экране только то, что увидит игрок, как в PEAK. Никаких служебных цифр: панель со всеми числами физики спрятана, **F1** показывает и прячет её.

- **Слева внизу — одна полоска сил.** Светлая часть — сколько сил у тела сейчас. **Голод** (оранжевый), **холод** (голубой) и **сон** (сиреневый) — не отдельные полоски, а куски, откушенные от правого края этой же: чем больше накопил, тем короче отрезок, который можно восстановить отдыхом. Над каждым куском его имя. Съеденная шоколадка (**E**) убирает часть голода и приклеивает к правому краю зелёный кусок — бонус, который тратится первым и не восстанавливается.
- **Справа внизу — три слота и рюкзак**, как в PEAK. В слотах — то, что под рукой: день начинается с **фонариком** в первом слоте и **шоколадкой** во втором; в рюкзаке — стартовый набор ночи (сухари, сало, тушёнка, спички, топор и т. д., `Items.StarterFor`) и ещё две шоколадки. **1, 2, 3** берут слот в руки (ещё раз — убирают), **0** или **Q** — пустые руки. **F** — свет: фонарик сам берётся в руки; **E** — съесть шоколадку, где бы она ни лежала; **клик** — использовать то, что в руках. Фонарик — тот же, что в ночной игре: модель из `Resources` (ItemsFactory) и луч `Torchlight/TorchBeam`, общий для игры и песочницы, с теми же двенадцатью минутами батареи.
- **Tab — рюкзак.** Окно со списком вещей, объёмом и весом; клик по вещи — в свободный слот, клик по слоту внизу — обратно в рюкзак; если слоты заняты или рюкзак полон, экран так и скажет. Tab или Esc закрывает. Правила набора — `Core/Kit.cs` (три слота + `Backpack` игры, тесты `KitTests` в dotnet).
- **Конец дня.** Когда голод, холод и сон съели всю полоску (стоя на снегу — минут пять), тело падает и не встаёт: экран темнеет, «СИЛ НЕ ОСТАЛОСЬ», кнопка «НАЧАТЬ ЗАНОВО» (или Enter, Space, F2) — новый день на том же стенде: полоска целая, рюкзак собран заново. Как в PEAK.
- **Прыжок** (Space) больше не требует восьми единиц сил: с почти пустой полоской тело всё равно подпрыгивает, только ниже (до половины высоты). Раньше после пятнадцати секунд бега бежать ещё было можно (бег держится до единицы), а прыгать — нет, и пробел просто молчал.
- Клавиши на экране не пишутся; при старте одна строка подсказки на четыре секунды.

| Клавиша | Действие |
|---|---|
| WASD / стрелки, Shift, Space | идти, бежать, прыгать |
| мышь, V (или F7, F3) | взгляд; первое / третье лицо |
| 1 / 2 / 3, 0 или Q | слот в руки / убрать; пустые руки |
| F, E, клик | фонарик вкл/выкл; съесть шоколадку; использовать то, что в руках |
| Tab | рюкзак (Esc или Tab — закрыть) |
| Enter, Space, F2 | после конца дня — начать заново (или кнопка на экране) |
| F1 | панель всех чисел тела |
| F2 | пересобрать тело на месте |
| F4 / F5 / F6 | замедление ×4; сохранить набор; загрузить набор |
| F8, F10 | руки (черновик); покачивание головы |
| F11 / F12 | следующий / предыдущий стенд (список стендов — в панели F1) |
| Esc | отпустить курсор |
| F9 | в меню игры |

Правила полоски — `Core/Condition.cs` (без движка, тесты `ConditionTests` идут в dotnet): `Add`/`Ease` кусков, `Tick` с давлениями (голод всегда, холод от снега под ногами и стоя в сугробе втрое, сон медленно), потолок = 100 − сумма кусков, ниже нуля не уходит. Тело (`Puppet.Ceiling`, `Puppet.Extra`) получает потолок каждый кадр и не восстанавливает силы выше него; бонус тратится раньше полоски (`Puppet.Pay`). Это первый кусок бэклога E0 («одна полоска»).

### Запуск

**Основной способ — редактор.** Меню **1079 → Physics sandbox → Open scene**, затем Play. Запуск мгновенный, ползунки крутятся на ходу, мир не генерируется.

**Сборкой** — когда надо проверить вживую или отдать соавтору:

```bash
Tools/mac/sandbox.command            # собрать и запустить
Tools/mac/sandbox.command --run      # запустить уже собранное
Tools/mac/sandbox.command --selftest # прогнать проверку физики и напечатать вердикт
```

Логи — `sandbox-editor.log`, `sandbox-player.log`. Редактор должен быть закрыт.

Честная цифра: сборка идёт около **четырёх минут** и весит **около 500 МБ**. Не потому, что в неё попадает мир как сцена — её сцена пустая, — а потому что Unity кладёт в любой плеер всю папку `Assets/Resources`, а там лежит сгенерированная гора. Убрать это можно, только вынеся мир из `Resources`, и это отдельная работа. Поэтому цикл «правка → проверка» держим в редакторе, а сборку делаем редко.

На Windows: `powershell -ExecutionPolicy Bypass -File Tools\win\sandbox.ps1` (по образцу мак-версии, вживую не запускалось).

### Проверка физики без глаз

`--selftest` водит тело по стендам скриптом и пишет в лог, что получилось. Код выхода 0 — всё сошлось. Проверяется только то, что делают ноги.

Это не украшение. Самотест поймал четыре вещи, которых не видно глазом: пружина ног после падения выстреливала телом на два метра вверх, и «удар» записывался от этого прыжка, а не от падения; на 60° тело съезжало шагом, потому что та же пружина гасила вертикальную скорость, то есть сам спуск; столбик таблички стоял ровно на линии ходьбы и останавливал мягкое тело (жёсткое его проталкивало); а замер подъёма по ступеням смотрел на высоту в конце четвёртой секунды, когда тело успевало подняться и сойти с лестницы.

Текущий прогон:

```
ok · стоит на опоре · опора 0.98 м, уклон 0°
ok · не проваливается и не подпрыгивает · Δy 0.000 м
ok · не ползёт стоя · снос 0.000 м за 1,5 с
ok · идёт с заданной скоростью · 2.89 м/с при шаге 3.00 м/с
ok · поднимается по ступеням 0,2 м · поднялся на 0.60 м
ok · поднимается по склону 30° · набрал 4.18 м за 3,5 с
ok · в гору медленнее, чем по ровному · 2.13 против 2.89 м/с
ok · держится на 46° (предел 48°)
ok · не сползает стоя на пределе
ok · на 60° тело соскальзывает · скользило 2.5 с из 2,5
ok · соскальзывание уносит вниз по склону · 2.1 м, вниз до 6.8 м/с
ok · скорость скольжения ограничена · 3.8 при пределе 9.0 м/с
ok · при скольжении тело держится склона, а не летит
ok · падение с 17 м засчитано целиком · удар 18.0 м/с
ok · ноги не подбрасывают тело после падения
ok · встаёт после падения
ok · падение валит тело на бок · корпус уходил на 100° от вертикали
ok · встаёт не рывком · выпрямился через 3.2 с после удара
ok · удар сажает тело на ноги · колени подались на 0.14 м при ударе 5.8 м/с
ok · приседание разгибается не мгновенно · держалось 0.23 с
ok · тело при этом действительно просело · опора падала до 0.56 м
ok · и снова встаёт в рост · опора 0.98 м
ok · корпус заваливается вперёд на разгоне · 16.1°
ok · и откидывается назад на остановке · −16.5°
ok · идёт вбок, не разворачиваясь по направлению шага · корпус ушёл от взгляда на 0°
ok · стоя, тело не крутится за камерой со стороны · камера ушла на 120°, корпус — на 0°
ok · первый шаг разворачивает тело за камерой · корпус в 0° от взгляда
ok · и уводит туда, куда смотрит камера · прошёл 5.7 м, отклонение от взгляда 0°
ok · изнутри головы взгляд поворачивает тело и стоя · корпус в 4° от взгляда
```

### Как посмотреть глазами

```bash
Tools/mac/sandbox.command --shot
```

Двенадцать кадров — стоит, в шаге, склон 30°, соскальзывание с 60°, вид от первого лица стоя и в шаге (с руками в кадре), сугробы, прыжок, стойка с шагом спереди, стойка сзади (рюкзак) и экран с полоской и слотами — лягут в `Builds/sandbox/shots/`. Полезно, когда надо показать фигуру, не садясь за машину.

### Настройки

Все числа тела правятся ползунками вживую и сохраняются в JSON:
`~/Library/Application Support/<company>/<product>/puppet-sandbox.json` (путь показан наверху панели F1).

Файл — это и есть результат работы в песочнице. Игра читает такой же файл по имени набора (`PuppetTuning.Load("...")`), так что найденное ощущение переносится без правки кода.

## 2. Что на самом деле в PEAK

Разбор по открытым источникам (обсуждение на Unity Discussions, вики игры, материалы разработчиков) — [ссылки в конце](#источники).

- **Активного рэгдолла нет.** Игрок управляет по сути **двумя руками и двумя ногами**; они двигаются напрямую. Тело, которое видят другие, — **пассивная кукла**, которую тянет за конечностями и которая старается занять правильную позу.
- **Пружины вместо приводов суставов.** Плавность даёт spring physics, а не PD-контроль на каждом суставе. Поэтому симуляция дешёвая и не разваливается при тормозах и сетевой задержке.
- **Один глагол — лезть, одна шкала — стамина.** Хват держится, пока есть силы; кончились — руки разжимаются и человек летит. Все состояния (голод, травма, холод, вес) не отдельные полоски, а **куски, откусанные от той же полоски** (это же записано у нас в [BACKLOG.md](BACKLOG.md)).
- **Восстановление — только на опоре**, после короткой паузы, и заметно быстрее, чем трата.
- **Кооперация физическая, а не через меню**: протянутая рука другого игрока — это реальная точка хвата.

Почему это важно: «повторить PEAK» обычно означает «написать active ragdoll», а это месяцы и нестабильность. На деле нужен набор Rigidbody и правильно подобранные силы.

## 3. Как устроено наше тело (`Assets/Scripts/Puppet`)

Сборка `Height1079.Puppet` не зависит ни от сети, ни от `Core`, ни от игры — только от UnityEngine. Поэтому её гоняют одинаково песочница, тесты и игра.

| Файл | Что делает |
|---|---|
| `Puppet.cs` | тело: парение над опорой, стойка, шаг, уклоны, соскальзывание, падение, одна полоска сил |
| `PuppetFigure.cs` | то, что видно: фигура из круглых форм, походка, двухкостный IK на колени и локти |
| `PuppetArm.cs` | рука как одна поверхность: труба от плеча через скруглённый локоть до ладони, перестраивается каждый кадр; из конца ладони растут три пальца и большой |
| `PuppetMesh.cs` | токарный станок: контур, прокрученный вокруг оси (`Revolve`), и тот же контур, протянутый вдоль кривой (`Sweep`) |
| `PuppetSkin.cs` / `PuppetSkinTexture.cs` | детали фигуры, которые точатся один раз, и атлас, нарисованный кодом |
| `PuppetHand.cs` | рука (черновик, выключен): тянется, смыкается, держит тело, рвётся |
| `PuppetRig.cs` | сборка тела |
| `PuppetTuning.cs` | все числа + сохранение/загрузка |
| `PuppetInput.cs` | что телу приказали в этот шаг |
| `Grip.cs` | свойства поверхности |

### Камера и разворот тела

Тело не поворачивается к направлению шага — только ко взгляду (`Puppet.Upright`): нажал «влево» — идёт приставным шагом, глядя туда же, куда смотрел. Так в PEAK, и на этом держится вся походка фигуры (`Facing`, `Drift`).

Когда тело разворачивается за камерой, а когда нет — решает тот, кто держит камеру, флагом `PuppetInput.FreeLook`:

| Вид | Стоит на месте | Идёт, висит на руках, скользит |
|---|---|---|
| от первого лица (`FreeLook` выключен) | тело поворачивается за взглядом сразу | поворачивается за взглядом |
| со стороны (`FreeLook` включён) | камера ходит вокруг, тело стоит как стояло | первый шаг разворачивает тело за камерой, дальше оно идёт за ней |

Стоя тело держит последний курс, к которому его повернули (`Puppet.HoldingHeading`), а не «выключает поворот»: иначе любой толчок из мира закрутил бы его. Это стандартная схема сторонних камер (GTA, «Ведьмак», Zelda): обзор всегда свободен, а модель поворачивается только по делу. Проверяется самопроверкой (`--selftest`, шаг 11b).

**Руки в первом лице.** Изнутри головы фигура спрятана (`PuppetFigure.SetVisible(false)`), но две руки остаются (`ShowEyeArms`): те же трубки от плеча до пальцев, что и в третьем лице, только подвешены к глазу, а не к плечам тела (`PuppetFigure.PoseFromEye`). Ладони держатся впереди и врозь, в нижних углах кадра, локти уходят за край картинки; мах — тот же, что у рук тела (тот же такт шага, тот же размах от длины шага), в прыжке руки поднимаются, при включённых физических кистях (F8) каждая рука тянется к своей кисти. Кисти чуть отстают от поворота головы (не более 6 см) — от этого они читаются как свои, а не нарисованные на объективе. Позу задаёт тот, кто ставит камеру, **после** её установки (`SandboxCameraRig.Aim`): подвешенные к камере прошлого кадра руки дрожат.

### Ноги — из чего состоит ходьба

1. **Тело парит на пружине, а не стоит капсулой.** Туловище держится на `HoverHeight` над тем, что нашёл щуп. Ступени до 0,35 м и камни проглатываются сами, ничего не цепляется за кромки.
2. **Пружина ног ограничена четырежды:** потолок ускорения, предел скорости подъёма, выход на силу за `LegRise`, и приседание при ударе. Без первых трёх она катапульта — лежащее тело подбрасывает на два метра.
3. **Уклон стоит денег.** Вверх шаг падает до `UphillSpeed`, вниз растёт до `DownhillSpeed`, по грейду под следующим шагом.
4. **Круче `FootGrip` (48°) — соскальзывание, а не падение.** Тело прижато к склону, едет по линии падения с трением, упирается в предел скорости, немного руля остаётся игроку.

### Резиновость: почему раньше было похоже на робота

Разбор Human Fall Flat, Gang Beasts, Fall Guys и PEAK дал один вывод: ощущение веса создаёт **не управление суставами, а запаздывание**. Видимое тело всегда догоняет цель, которой не успевает достичь, и зазор между ними глаз читает как массу. Полный active ragdoll (скелет на joint-ах с PD-приводами) — дорогая и неустойчивая вещь; нам нужны были задержки, а не приводы.

Поэтому добавлено четыре слоя, каждый со своим числом и каждый выключается в ноль:

1. **Наклон корпуса — настоящий, в физике.** PD-вертикали выпрямляет тело не к мировой вертикали, а к оси, наклонённой в сторону собственного ускорения (tan θ = a/g). Одна формула даёт наклон вперёд на разгоне, откидывание назад на торможении и крен в поворот. Ускорение измеряется, а не берётся из команды, — значит толчок от мира кренит тело так же, как шаг. Пружина вертикали размягчена (было 110/16, стало 70/9): корпус проскакивает цель и садится обратно.
2. **Пружинистые ноги.** Удар тянет вниз саму высоту парения, и она разгибается с постоянной скоростью — глубокое приземление и глубже, и дольше, из одного числа. Колени сгибаются сами, потому что фигура читает высоту опоры.
3. **Настоящее падение.** Выше `LimpFrom` тело обмякает: вертикали нет, ноги выключены, и — главное — **тело бросается через линию движения** угловым импульсом. Без этого обмякшая капсула просто стояла с выключенным управлением, что и читалось как «падений нет». На время падения капсула надевает шершавый материал, встаёт за `GetUp` секунд, не рывком. Ниже порога — спотыкание на долю секунды.
4. **Размеренность.** Потолок ускорения разделён на три: трогание с места, установившийся шаг, торможение.

Попутно исправлено: падением считалось любое соскальзывание, поэтому тело валилось с ног внизу каждого жёлоба.

### Фигура: как она устроена

Фигура — **процедурный меш с процедурной текстурой**, целиком в коде, без единого ассета.

**Форма.** Меш в Unity — это четыре списка: точки, треугольники, нормали и UV. Каждая деталь вытачивается как на токарном станке: рисуется плоский контур «радиус / высота» и прокручивается вокруг оси. Сам контур задан несколькими точками, через которые протянут сплайн, — значит силуэт остаётся коротким списком чисел, который правится на глаз, а поверхность выходит гладкой. Голова — не шар, а формула капли, у которой самое широкое место ниже середины.

**Одежда.** Одежда — не отдельные фигуры поверх тела, а **один скиннованный меш** (skinned mesh: поверхность, каждая точка которой привязана к одной-двум костям и следует за ними), как в любой игре с персонажами. Куртка с рюкзаком, седалище штанов и две штанины — одна непрерывная поверхность на костях фигуры: тело, два бедра, две голени (две плечевые кости в списке остались, но ничего не несут). Штанина «протягивается» (тот же станок, но ось отпущена: цепочка колец со своими центрами и радиусами) изнутри седалища через тазобедренный сустав до ботинка; на колене три кольца поделены между бедром и голенью 20/50/80 %, поэтому ткань гнётся, а не показывает две трубы со щелью между ними. Каждый край заканчивается подворотом внутрь — у него есть толщина. Куртка зимняя, дутая: бочка шире штанов (0,255 м у подола против 0,185 у пояса), подол свисает поверх пояса на 2 см, воротник-стойка кольцом вокруг низа головы; молния, карманы и вязаный подол — краска. Рукава куртки — это сами руки: труба руки, перестраиваемая каждый кадр (`PuppetArm`), точится с радиусами рукава (12,8 см у плеча, 9,8 у запястья), ступенькой садится на вязаную манжету 6,8 см, и из манжеты выходит кисть — кожей остаётся только она. Чтобы манжета сидела на запястье при любом сгибе локтя, колонка атласа для руки размечена в метрах **от кончиков пальцев**, а не долями длины трубы. Нижняя половина седалища следует за обоими бёдрами поровну: в приседе оно уходит вперёд вместе с ногами. Кожа (голова с шапкой, кисти, ботинки) — по-прежнему жёсткие детали на трансформах; тело, бёдра и голени сами ничего не рисуют, но считаются каждый кадр — это кости одежды. Чтобы скиннинг работал, у кости должна быть устойчивая рамка: Y вдоль кости, Z — к передней стороне тела (`PuppetFigure.Aim`); раньше поворот вокруг оси кости был произвольным и у ноги, проходящей через вертикаль, разворачивался кругом — жёсткой капсуле всё равно, рукав это выкручивало бы.

**Рюкзак.** Единственное на теле, что не круглое. Кольцо станка умеет быть не только эллипсом, но и прямоугольником со скруглёнными углами (`PuppetMesh.Station.Corner`, обход по длине дуги, чтобы нарисованная лямка была одинаковой ширины на грани и на углу), и рюкзак — три таких коробки: мешок 34×20×42 см за спиной, клапан сверху на сантиметр шире мешка и карман на лицевой стороне. Лицевой стороной он точится вперёд (там середина текстурной колонки) и разворачивается на 180°, чтобы шов текстуры лёг к спине, где его не видно. Лямки, пряжки, укреплённое дно и клапан кармана — краска, не геометрия.

**Шапка.** Вязаная: колпак в трёх сантиметрах от головы, отворот внизу (нижний край на палец выше бровей — глаза никогда не закрыты) и помпон. Отворот в атласе «в рубчик»: тёмная полоса с тонкими вертикальными линиями цвета шапки. На шапке и помпоне включена вязка ткани, крупнее, чем на куртке.

**Текстура.** UV получаются бесплатно из той же протяжки: одна координата идёт вокруг детали, вторая вдоль контура и измеряется **в метрах** — поэтому полоса на картинке сохраняет настоящую ширину на теле. Картинка одна на всего человека, атлас 1024×1024, разложенный на куски (голова, торс, колонки под штанину, седалище, руку, кисть и плечо рукава, ботинок, шапку с помпоном, две колонки под рюкзак с клапаном и карманом). Рисуется пикселями в коде тремя кистями: заливка, овал с мягким краем, линия из овалов. Из них собрано всё — глаза с бликами, брови, рот, молния, карманы и воротник куртки, манжеты, подвороты, пояс, лямки и пряжки рюкзака. Лямок на груди нет: нарисованные на куртке они читались как подтяжки. Положение манжеты, подворота и лямок атлас спрашивает у самой выкройки (`PuppetSkin.SleeveCuffV`, `PuppetSkin.PackV` и т. п.), так что при перекройке полоса остаётся на месте.

**Фактура ткани.** Плоский цвет на такой простой форме читается как пластик; у фигур PEAK на одежде мелкая регулярная вязка. Здесь это второй, крошечный рисунок 64×64 — плитка 4 см с шестнадцатью мягкими точками и лёгкой диагональю, — которую стандартный шейдер Unity накладывает как detail map через **второй набор UV**: станок кладёт его в метрах и оборачивает вокруг кольца целое число раз, поэтому стежок одного размера на куртке и на рюкзаке, а на штанах в полтора раза мельче, и шов нигде не виден. С кожи и ботинок вязку снимает альфа-канал атласа: он служит маской детали (1 на тканевых кусках и шапке, 0 на остальных; на колонке руки — 1 до запястья и 0 на кисти). Вариант шейдера с detail map попадает в сборку только если на него ссылается материал в `Resources`, поэтому `SandboxSetup` сохраняет `Resources/Sandbox/Cloth.mat` — без текстур, только ключевое слово, — и материал фигуры создаётся из него.

Скелета нет: поза считается в мировых координатах от физической капсулы, скиннинг — только у одежды, и висит она на тех же трансформах. Стойка — PD-регулятор, тело физически падает и встаёт, а не приклеено к вертикали.

**Постановка ног.** Первая фигура ходила по одной дорожке шириной 0,20 м — «люди ходят почти в одну линию», и это правда, но выглядело как кукла на рельсе: ноги слеплены, шаг строевой. Три числа стойки вынесены в настройки (раздел «шаг» на панели) и правятся вживую:

| Настройка | Ключ в JSON | По умолчанию | Что делает |
|---|---|---|---|
| ноги врозь, м | `StanceWidth` | 0,28 | расстояние между стопами: дорожка, по которой стоит и идёт фигура |
| носки наружу, ° | `ToeOut` | 8 | разворот ботинок; колено гнётся туда же, куда смотрит носок, а не строго вперёд |
| разброс шага, м | `StepScatter` | 0,04 | на сколько стопа может лечь мимо своей линии, внутрь-наружу и короче-длиннее; разыгрывается заново при каждом отрыве ноги, 0 — снова рельсы |

Разброс держится, пока нога стоит, поэтому остановившаяся фигура стоит так, как ноги легли, а не подравнивает их.

Одной ширины стойки оказалось мало: штанины, висевшие с бёдер, разнесённых на 0,22 м, у паха срастались в одну колонну. Первый ответ был развести суставы до 0,16 м и стойку до 0,42 при штанинах 0,24 м толщиной — и фигура стала ковбоем. Теперь наоборот: суставы на 0,13 м (`PuppetFigure.HipOut`), штанины 0,20 м толщиной, седалище 0,24 м в каждую сторону, стойка 0,28 — ноги висят почти отвесно и между ними остаётся просвет. Замер стойки печатается в лог кадров (`shots: стопы … м врозь`).

## 4. Что повторимо, а что нет

**Повторимо один в один:**
- вис, раскачка, подтягивание, срыв от нехватки сил;
- хват за подвижные предметы и за руку другого игрока;
- разная цена зацепа (лёд, крошащаяся порода, полка);
- падение и обмякание тела;
- одна полоска с откусанными кусками.

**Повторимо, но требует работы:**
- **скелет и анимация.** Сейчас руки и ноги — палки. Нужен пассивный рэгдолл на костях модели `hiker-v1.glb` с пружинами к целевой позе. Это следующий крупный шаг, и он чисто визуальный: физика от него не меняется.
- **ноги как отдельные точки.** У нас ноги — щуп и пружина, а не два управляемых тела. Для упора в стену и «лазанья ногами» их придётся сделать телами, как руки.
- **сеть.** Сейчас синхронизируется один трансформ. Придётся слать туловище + две кисти + флаги хвата (около 30 байт на игрока), остальное клиент досчитывает сам. У PEAK ровно та же логика — потому она и держит четверых.

**Не повторимо без переделки мира — и это главная развилка:**
- лазать не по чему. Unity Terrain — это поверхность без зацепов, а карабкаться надо по скалам с коллайдерами. У нас гора — рельеф плюс деревья и камни-декорации.
- либо на маршруте появляются **скальные участки как геометрия** (а это новая работа по карте), либо физика лазанья остаётся только для отдельных мест (скалы Пастухова, кулуары), а основное движение — по-прежнему тропёжка.

То есть **физика тела и карта развязываются песочницей, но в игре они снова встретятся**: PEAK-тело без PEAK-геометрии даёт хорошую ходьбу и падения, но не даёт лазанья.

## 5. Порядок работ

1. ☑ Песочница: сцена без мира и сети, полигон, панель всех чисел (спрятана, F1), самопроверка, съёмка кадров, вход из меню игры.
1. ☑ Экран как в PEAK: одна полоска сил с кусками голода, холода и сна (`Core/Condition`), бонус от еды, слоты и рюкзак (пустые).
2. ☑ Ноги: ходьба, бег, ступени, уклоны, соскальзывание, падение и вставание.
3. ☑ Резиновость: наклон корпуса в физике, приседание при ударе, настоящее падение с броском через себя, спотыкание, раздельные разгон и торможение.
4. ☑ Фигура: процедурный меш и процедурная текстура, нарисованное лицо, одежда одним скиннованным мешем с фактурой ткани — куртка с длинным рукавом, штаны, прямоугольный рюкзак с клапаном и карманом, вязаная шапка, ботинки.
5. ☑ Песочница как карта: первое лицо по умолчанию, покачивание головы, снег, сугробы, дневной свет. Следы — те же, что в игре (`Height1079.Snow`, см. [ATMOSPHERE.md](ATMOSPHERE.md#следы-и-снег-под-ногами-snowsnowprintscs)); по утоптанной тропе кукла идёт выше, чем по целине.
6. ☐ Подобрать числа руками и положить набор в репозиторий.
7. ☐ Рюкзак отдельной геометрией с лямками и клапаном; очки.
8. ☐ Разворот ладони при хвате; поза при скольжении.
9. ☐ Руки: довести черновик (F8) до виса, подтягивания и срыва.
10. ☐ Сеть: туловище + кисти + флаги, клиент досчитывает.
11. ☐ Врезать `Puppet` в `HikerController` за флагом, чтобы сравнить со старым телом.
12. ☐ Решение по карте: где на маршруте появляется скальная геометрия под лазанье.

## Источники

- [How does character controller work in PEAK game? — Unity Discussions](https://discussions.unity.com/t/how-does-character-controller-work-in-peak-game/1683399) — про две руки, две ноги и пассивное тело на пружинах.
- [PEAK Wiki — How to play](https://peak.wiki.gg/wiki/How_to_play) — хват, стамина, восстановление на опоре.
- [PEAK Stamina Guide](https://www.switchbladegaming.com/peak/stamina-guide/) — траты и восстановление стамины.
- [How co-op climbing hit Peak achieved 2 million sales — Game Developer](https://www.gamedeveloper.com/production/how-co-op-climbing-hit-peak-achieved-2-million-sales-for-less-than-200-000-) — про масштаб и подход команды.
