# Бэклог механик: «1079» в парадигме PEAK

Источник: ресёрч Антона («Как это может выглядеть в парадигме PEAK», сентябрь 2026) + разбор самой PEAK (Aggro Crab / Landfall, 2025). Бэклог — рабочий; статусы правим прямо здесь. Реализация — Unity-клиент (`1079-unity`), логика правил — в `Assets/Scripts/Core` без UnityEngine, чтобы гоняться в `dotnet`.

## 1. Что именно работает в PEAK (и что берём)

PEAK держится на одном физическом глаголе — лезть — и одной шкале — стамине. Всё остальное подвешено к этой шкале, а не к отдельным индикаторам:

- **Одна полоска.** Стамина тратится на лазанье, бег и прыжки, восстанавливается 20/с на земле после короткой паузы. Все состояния (голод, травма, холод, жар, яд, сонливость, вес) не отдельные бары, а **куски той же полоски**, отрезанные от максимума: чем больше накопил, тем короче твой «рабочий» отрезок. Статусы капают тиками по 2,5, потолок 200 %.
- **Травма не проходит сама.** Падение даёт 5–100 очков травмы, они не регенерируют — только костёр, бинты, еда. Холод и жар со временем уходят сами, если убрать причину; голод растёт 0,05/с всегда.
- **Костёр — узел кооперации и сохранение.** Зажечь можно только когда **все** игроки в радиусе ~19 м; зажжённый костёр лечит 20 травмы, даёт +25 бонусной стамины, останавливает голод и туман, открывает следующий биом и автосохраняется. Уйти от костра дальше радиуса — таймер 60 с и он гаснет.
- **Друг к другу.** Холод снимается «кучкованием»: 1,5/с в пределах 3,2 м от другого игрока. Верёвки и крючья ставит один, чтобы прошли остальные. Ничего не «запрещено соло», просто соло дороже.
- **Давление времени — не таймер смерти, а стена тумана**, которая на высоких сложностях поджимает снизу.
- **Вес** — просто ещё один кусок полоски; на скорость не влияет.

Что из этого мы берём буквально: одну полоску состояния с «откусанными» кусками, костёр/печку как узел «все вместе», кучкование от холода, туман → у нас *график*. Что не берём: лазанье (у нас его нет — есть тропёжка), аркадные предметы, ползучие враги.

Наша текущая ночь уже устроена похоже (`SurvivalRules`: тепло/руки/ясность, розжиг, шторм, исходы пары), но это три отдельных бара. Первое решение бэклога — свести их в **одну полоску «сил»** с откушенными кусками (холод, усталость, вес, травма). Это и есть перевод на язык PEAK.

## 2. Глаголы

| PEAK | 1079 | Чем оплачивается | Кооперация |
|---|---|---|---|
| лезть | **тропить** (идти первым по глубокому снегу) | силы ведущего; ведомые по лыжне почти бесплатно | смена ведущего стоит времени и «пересборки» цепочки |
| ставить верёвку/крюк | **ставить палатку вдвоём** (стойка + оттяжка), **печка из колен** | время, руки на ветру | одна пара рук физически не может |
| костёр | **печка/костёр + все рядом** | дрова, розжиг (уже есть) | как в PEAK: собраться, иначе не горит толком |
| падение → травма | **промокнуть → сушиться → сжечь вещь** | вещь теряется навсегда | развесить может любой, следит дежурный |
| туман снизу | **график перехода** (план vs факт) | опоздал → срезаешь → встаёшь не там | решение всей группы |
| — | **ночное дежурство** (спит/дежурит, смена раз в час) | сон восстанавливает, дежурство держит печку | асимметрия — уникальна для ниши |

Порядок по Антону: сначала **тропёжка со сменой ведущего** и **палатка вдвоём**. Если эти два глагола не «ощущаются» — остальное не спасёт. Бэклог построен так, чтобы к концу M1 это можно было проверить на четверых.

## 3. Модель состояния (фундамент, делается первым)

**Одна полоска «силы» 0–100.** Максимум откусывают состояния (в тиках по 2,5, как в PEAK): холод, усталость, вес, травма, промокание. Рабочий отрезок = 100 − сумма кусков. Тратится тропёжкой, работой руками на ветру, бегом. Восстанавливается: стоя/на лыжне (медленно), у печки (быстро), во сне (полностью, но только во время сна).

- Холод: растёт от ветра/мокрой одежды/стояния, уходит у печки, от движения, от кучкования (≤3 м от товарища).
- Усталость: растёт от тропёжки (у ведущего ×1, у ведомых ×0,3), уходит только сном/отдыхом у печки.
- Вес: константа от рюкзака; снимается лабазом или передачей вещей.
- Промокание: от глубокого снега без гамаш, от наледи; уходит только сушкой.
- Травма: падения, ожог, обморожение рук; уходит только на ночёвке.

«Ясность» текущей ночи (тряска, туман в глазах) остаётся производной: когда рабочий отрезок < 25 % — камера, звук, скорость. «Руки» превращаются в холод-состояние + отдельный штраф на действия руками (розжиг, оттяжки, карта).

## 4. Эпики и истории

Размер: S — до дня, M — 2–3 дня, L — неделя. «Core» — правило в `Assets/Scripts/Core` с тестом; «Rt» — Unity-часть. Статусы: ☐ не начато, ◐ в работе, ☑ готово.

### E0. Одна полоска (фундамент)

| # | История | Приёмка | Размер |
|---|---|---|---|
| ◐ 0.1 | Core: `Condition` — полоска 100 с кусками (cold, fatigue, weight, wet, injury), `Work(cost)`, `Rest(dt, mode)`, тики по 2,5, потолок 100 | тесты: куски не дают рабочему отрезку уйти ниже 0; сон снимает fatigue, не снимает injury; кучкование снимает cold 1,5/с в радиусе 3,2 м | M |
| | *Сделано (19.09.2026):* `Core/Condition` с кусками hunger, cold, sleep, `Add`/`Ease`/`Tick`, потолок не ниже 0, тесты в dotnet; тело песочницы (`Puppet.Ceiling`, `Extra`) живёт по этому потолку, бонус от еды тратится первым. Не сделано: weight, wet, injury, тики по 2,5, `Work`/`Rest` (траты пока внутри `Puppet`). | | |
| ☐ 0.2 | Core: перевод `SurvivalRules.Tick` на `Condition` (тепло → cold, руки → штраф действий, ясность → производная) с сохранением текущих исходов и тестов | все 17 тестов зелёные; протокол ночи не меняет формат | M |
| ◐ 0.3 | Rt: HUD — одна полоска с цветными откушенными кусками (как в PEAK), подписи кусков при наведении/паузе | видно на 1280×800 и в фуллскрине; куски читаются без легенды | S |
| | *Сделано (19.09.2026):* полоска с кусками и подписями, зелёный бонус, три слота и рюкзак — в песочнице (`Sandbox/SandboxHud`). В ночной игре HUD прежний. Слоты и рюкзак живые: `Core/Kit` (три слота + `Backpack` игры, тесты), окно рюкзака по Tab, фонарик игры в руке (луч вынесен в общий `Torchlight/TorchBeam`), конец дня при съеденной полоске с экраном «Начать заново». | | |
| ☐ 0.4 | Сервер-авторитарность: `Condition` считает `NightRun` на хосте, клиенты получают снапшот 4 Гц (как сейчас heat/hands) | второй экземпляр видит ту же полоску с задержкой ≤ 250 мс | S |

### E1. Тропёжка (главный глагол)

| # | История | Приёмка | Размер |
|---|---|---|---|
| ☐ 1.1 | Core: карта глубины снега — слой поверх DEM: наст (0–0,15 м), целина (0,3–0,6), распадок (0,8–1,2); генератор от кривизны/уклона/ручья + ручные пятна; `SnowDepth(x,z)` | тесты: у ручья глубже, на гребне наст; детерминированно от seed | M |
| ☐ 1.2 | Core: стоимость шага = f(глубина, вес, наклон); «след» — полилиния с шириной 0,6 м и временем жизни (заметается ветром за N минут); шаг по следу = 30 % стоимости | тест: второй по следу тратит ≤ 1/3 первого; след через 10 мин шторма исчезает | M |
| ☐ 1.3 | Rt: движение по глубине — скорость и анимация (по щиколотку / по колено / по пояс), лыжи и без лыж; след рисуется на террейне (decal/вторая текстура) | на глаз три режима отличимы; след виден сзади идущему | L |
| ☐ 1.4 | Смена ведущего: обгон физически (сойти со следа = целина), «пропусти» — жест/кнопка, ведущий отступает, следующий тропит; смена стоит 5–8 с стояния всей цепочки | тест на Core: очередь ведущих, штраф времени; в игре четверо могут меняться без слов | M |
| ☐ 1.5 | Метрика для плейтеста: расстояние/минута группы, кто сколько тропил, сколько раз менялись — в протокол ночи | строки в протоколе; есть в `Protocol()` пролога тоже | S |
| ☐ 1.6 | Обход по твёрдому: гребни/наст читаются глазом (цвет, блеск, звук); длиннее, но дешевле | плейтест: половина групп выбирает обход хотя бы раз | S |

### E2. Вес и лабаз

| # | История | Приёмка | Размер |
|---|---|---|---|
| ☐ 2.1 | Core: инвентарь группы — предметы с массой (продукты по дням, дрова, печка, запасная одежда, лыжи, топор, аптечка); вес участника → кусок полоски | тест: 25 кг = 20 % полоски; передача предмета переносит кусок | M |
| ☐ 2.2 | Core: лабаз — точка на карте, куда группа складывает предметы; вернуться можно; документальный состав (см. PROLOGUE M8) как пресет | тест: после лабаза суммарный вес ≤ порога; протокол пишет, что оставили | S |
| ◐ 2.3 | Rt: физические предметы в руках/на рюкзаке, передача из рук в руки (как в PEAK), сброс рюкзака при тропёжке «по Дятлову» | четверо передают дрова по цепочке без меню | L | Сделано: рюкзак с объёмом и весом, предметы в руках, снять/надеть, чужой рюкзак, вес замедляет, топор и пила, дрова из веток, охапка в руках, спички кончаются и мокнут (docs/ITEMS.md). Осталось: передача из рук в руки, связь с тропёжкой |

### E3. Лагерь: всё делается в четыре руки

| # | История | Приёмка | Размер |
|---|---|---|---|
| ☐ 3.1 | Площадка: утоптать (тропёжка на месте) и выкопать (лопата/лыжа) — ровность 0–1 по локальному рельефу и вытоптанности | Core-тест: ровность растёт от работы, зависит от уклона; ≥ 0,7 нужно для палатки без штрафа | M |
| ◐ 3.2 | Палатка вдвоём: стойка держится игроком (руки заняты), второй ставит лыжу-кол и тянет оттяжку — физическая верёвка (Rope, joints); натяжение 0–1 сохраняется | одному не поставить: без второго стойка падает; натяжение видно по провису | L | Сделано на Эльбрусе: палатка-двойка как носимый предмет, ставится и сворачивается за время стоя, правила размещения в `Core/Elbrus/Camp.cs` с тестами. Осталось: физика оттяжек и «в четыре руки» на Холатчахле |
| ☐ 3.3 | Ночь под слабой оттяжкой: провис → снег на скате → давит (полоска: cold ↑, спальное место сужается) → к утру может лечь | Core-тест: натяжение < 0,5 + снегопад → «палатка легла» событие; протокол | M |
| ☐ 3.4 | Печка из колен: 4–6 сегментов трубы, стык за стыком, вырез в стенке; ошибка стыка → дым внутрь (cold не падает, clarity ↓); закрытый тамбур на ночь → угар (сон не восстанавливает, событие) | Core-тест: последовательность стыков; Rt: сборка руками у двух игроков | L |
| ☐ 3.5 | Подстилка: слои (рюкзаки/телогрейки/лапник/ничего) → коэффициент теплопотерь за ночь; цифра не показывается, узнаётся утром (полоска) | тест: «ничего» = −40 % сил к утру, лапник = −10 % | S |
| ☐ 3.6 | Полог из простыней против конденсата: снижает wet за ночь | тест; протокол | S |
| ☐ 3.7 | Печка/костёр как узел: греет только тех, кто в радиусе; «все рядом» ускоряет розжиг (как костёр PEAK — все в 19 м) | тест: 4 у печки → розжиг ×2 быстрее | S |

### E4. Сушка как ловушка

| # | История | Приёмка | Размер |
|---|---|---|---|
| ☐ 4.1 | Core: мокрые вещи (варежки, фуфайка, носки, валенки) — wet 0–1 на предмете; сушка = расстояние до огня × время; ближе порога → шанс сгореть растёт по экспоненте | тест: на 0,4 м — потеря за 3 мин; на 1 м — сухо за 12 мин, риск 0 | M |
| ☐ 4.2 | Дрова: ель стреляет искрами и капает смолой (риск прожечь палатку/вещь ↑), берёза/сушняк спокойнее; выбор дров при заготовке | тест: ель ×3 риск | S |
| ☐ 4.3 | Rt: вешалка у огня — физически развесить, вещь видно; сгоревшая вещь исчезает с событием в протокол («сгорели две рукавицы и телогрейка» — документальный эпизод 28.01) | четверо теряют вещи по невнимательности, не по рандому | M |
| ☐ 4.4 | Потерянная вещь = постоянный кусок cold (без варежек — руки, без фуфайки — тело) до конца ночи | тест | S |

### E5. Ориентирование руками и график

| # | История | Приёмка | Размер |
|---|---|---|---|
| ☐ 5.1 | Карта — предмет: достать (2 с), развернуть (руки заняты, скорость 0), подсветить (фонарь у товарища), сверить с компасом; на ветру > порога — рвёт (событие, карта повреждена на участке) | Rt-проверка вдвоём: один светит, другой читает | M |
| ☐ 5.2 | Мёрзлые руки (cold > 40) — карту не развернуть, компас дрожит | тест | S |
| ☐ 5.3 | Core: **график** — план (контрольные точки со временем по дневникам) vs факт; отставание в минутах, показывается как полоска-тень, не как таймер | тест: план из PROLOGUE M7–M8; отставание считается от последней точки | M |
| ☐ 5.4 | Отставание → «срезать»: короткие альтернативы маршрута (склон вместо леса) видны только когда опаздываешь; выбор группы; последствия — где ночуешь | Core: граф маршрута с ветками; Rt: указатели | M |
| ☐ 5.5 | Протокол пишет: план, факт, где срезали, где встали | строки | S |

### E6. Ночное дежурство

| # | История | Приёмка | Размер |
|---|---|---|---|
| ☐ 6.1 | Core: роли sleep/watch на ночёвке, смена раз в «час» (игровые 3–4 мин); спящий восстанавливает силы полностью за смену, дежурный держит печку (дрова, стыки) и не даёт погаснуть | тесты: печка без дежурного гаснет за N мин; сон без печки = cold ↑ | M |
| ☐ 6.2 | Rt: спящий — экран тёмный, слышит приглушённо; дежурный видит/слышит всё; смена — разбудить рукой | вдвоём: один спит, второй дежурит, меняются | M |
| ☐ 6.3 | Ветки угроз через дежурного: провис палатки (3.3), дым (3.4), угар, снегопад, вещь у огня (4.x), «звук снаружи» (только как звук, без версий и объяснений — правило пролога) | по одному событию на смену максимум; всё документально-нейтрально | M |
| ☐ 6.4 | Асимметрия в протоколе: кто дежурил, что заметил, что проспали | строки | S |

### E7. Инфраструктура под всё это

| # | История | Приёмка | Размер |
|---|---|---|---|
| ☐ 7.1 | Rope/joints: физическая верёвка (оттяжка, связка) с сетевой синхронизацией концов | 2 игрока тянут одну верёвку без рассинхрона | L |
| ☐ 7.2 | Предметы в руках + передача (общий компонент для E2/E3/E4/E5) | | M |
| ☐ 7.3 | Ragdoll/падения → injury (как в PEAK 5–100 по времени падения) | тест на порог 1,5 с | M |
| ☐ 7.4 | Плейтест-сборка на четверых: лобби, имена, протокол на всех | Steam AppID 480 или ручной IP | M |
| ☐ 7.5 | Телеметрия плейтеста: кто сколько тропил, сколько раз менялись, сколько вещей сгорело, отставание — в файл рядом с протоколом | | S |

### E8. Физика тела по образцу PEAK (новая ветка, 19.09.2026)

Раздел 1 говорил «лазанье не берём». Пересмотр: берём не лазанье как режим игры, а **телесность** — тело, которое физически стоит, ходит по склонам, буксует на крутизне и падает. Порядок: сначала ноги, руки потом. Разбор PEAK показал, что там нет активного рэгдолла: две руки, две ноги и пассивное тело на пружинах, то есть это несколько Rigidbody, а не месяцы симуляции. Подробности и что именно повторимо — [docs/PHYSICS.md](PHYSICS.md).

| # | История | Приёмка | Размер |
|---|---|---|---|
| ☑ 8.1 | Песочница: отдельная сцена без мира и сети, полигон (ступени, склоны, стена, карниз, обрывы, подвижные предметы), панель всех чисел вживую, сохранение набора в файл | `Tools/mac/sandbox.command` собирает и запускает за секунды; числа правятся мышью | M |
| ☑ 8.2 | Тело `Height1079.Puppet`: ноги на пружине, PD-стойка, уклоны, соскальзывание, падение и вставание | самопроверка `--selftest` проходит целиком; EditMode-тесты на сборку тела | M |
| ☑ 8.3 | Фигура по образцу PEAK: голова в четверть роста, круглые формы, плоский цвет, мягкий свет, походка от метража | `--shot` даёт четыре кадра, на которых видно человека, а не примитивы | M |
| ☐ 8.4 | Подобрать числа руками в песочнице и положить файл настроек в репозиторий | ходьба и склоны ощущаются, числа в git | S |
| ☐ 8.5 | Лицо текстурой (глаза, рот) и поза при скольжении/падении | видно, куда смотрит и что с ним происходит | S |
| ☐ 8.6 | Руки: довести черновик (F8) до виса, подтягивания и срыва | стена 12 м проходится на 2/3 полоски | M |
| ☐ 8.7 | Ноги как два управляемых тела: упор в стену, шаг по зацепам | камин (стенд 6) проходится ногами | M |
| ☐ 8.8 | Сеть: туловище + две кисти + флаги хвата (~30 байт), остальное клиент досчитывает | четверо на одной стене без рассинхрона | M |
| ☑ 8.8a | Следы, утоптанный снег и карта троп — одна система на оба тела (`Height1079.Snow`); в песочнице след ложится на видимый снег, тропа поднимает куклу | в песочнице те же отпечатки, что в игре; EditMode-тесты | S |
| ☐ 8.9 | Врезать `Puppet` в `HikerController` за флагом, чтобы сравнивать со старым телом в одной сборке | переключение в настройках; старое тело работает как раньше | M |
| ☐ 8.10 | Решение по карте: где на маршруте появляется скальная геометрия с коллайдерами под лазанье | список мест в [docs/MAP.md](MAP.md) | M |
| ☑ 8.11 | Песочница как игра ([docs/SANDBOX.md](SANDBOX.md)): площадка 60×60 из объектов игры — костёр под кедром, палатка, укрытия; ночь, метель и пурга-стена по часам забега; Менк на свет / шум / следы (правила в Core с тестами); одна петля — донести печку из палатки до костра; присесть и лечь; экраны победы и смерти с итогом | песочница открывается на площадке; `--selftest` раздел 13; 14 тестов ядра | L |
| ☐ 8.12 | Площадка на четверых: правила уже принимают список игроков — нужна сеть (`NightSession`) и вещь «вдвоём» | четверо на одной площадке, свёрнутую палатку несут двое | M |
| ☐ 8.13 | Общие `Weather`/`Synth`/`NightFilm` (сборка `Night`) — перевести на них и `Ambience` (ветер сейчас продублирован в `WindSound`) | один код ветра на игру и песочницу | S |
| ☑ 8.14 | Тело носит любую скинованную модель со скелетом поверх позы `Puppet` (`PuppetSkeleton`, [docs/PHYSICS.md](PHYSICS.md) §3): кости по псевдонимам, наведение с поправкой на позу покоя, масштаб по росту, поза решается под пропорции модели. По умолчанию — Adventurer из Quaternius «Ultimate Modular Men» (CC0) в палитре команды (`FigureFactory`, `docs/art/palette.json`); B — hiker-v1 и процедурная фигура для сравнения; процедурная — запасная, под снос; физика и руки в первом лице не тронуты | стандартный набор `--shot` — с Adventurer, кадры 8, 10, 2 с суффиксом `-glb` — hiker-v1 с тех же точек; `--selftest` проходит | M |

## 4а. Эльбрус (вторая локация)

| | Что | Готово, когда | Размер |
|---|---|---|---|
| ☐ EL.1 | Локация согласуется по сети (хост объявляет её при подключении) | клиент в чужой локации не проваливается сквозь рельеф | S |
| ☐ EL.2 | Общие часы канатных дорог у хоста, ускорение — тоже по сети | второй игрок виден в той же кабине | M |
| ☐ EL.3 | Мини-карта и карта в руках для Эльбруса (сейчас картинка от Холатчахля) | на карте склон и станции | M |
| ☑ EL.4 | Высотная болезнь: акклиматизация, одышка, темп выше 4500 | тест на профиль подъёма | M |
| | Ядро (`Core/Elbrus/Ascent.cs`, `AscentCold.cs`, `AscentRoute.cs`) **подключено к рантайму**: хост держит `Climber` на участника и зовёт `Ascent.Tick` (`NightSession.Ascent.cs`), клиент считает точку маршрута и темп (`ClimbGear.cs`), HUD показывает часы, разворот, холод, ветер, руки/ноги/лицо и фазу горняшки. Бег выше 4600 м запрещён, кошки надеваются стоя за 12 с, срыв бросается хостом. Акклиматизация переносится между забегами. | | |
| ◐ EL.5 | Трещины, ледопад Гарабаши, следы ратрака и лыжные трассы на снегу | видно с канатки | M |
| | Собран `Elb_Ascent` (`Editor/World/ElbrusAscent.cs` + `.Saddle.cs`): 168 вешек, трос МЧС 4900–5250, верёвочные перила, колея ратрака, мульды и приоткрытые трещины справа от колеи, ручьи слева, скалы Пастухова с коридором, хижина Red Fox 5300 с фумаролой, приюты на морене. **Рантайм его создаёт** (`ElbrusWorld.RouteFurniture`), старые вешки через 55 м и заглушка хижины убраны. Осталось: ледопад и сераки под седловиной. | | |
| ☑ EL.7 | Прокат снаряжения внизу и жёсткий гейт выше скал Пастухова | без комплекта выше 4650 м не пускают, показывая чего не хватает | M |
| | `Core/Elbrus/Rental.cs` + `RentalService.cs`: тринадцать позиций = `Ascent.Required`, комплект 3900 ₽ из 7000 ₽ кошелька, стойки на Азау и Гара-Баши, покупка на E и цифрами, предметы приходят в рюкзак через хост. | | |
| ☑ EL.8 | Привал и сохранение: мини-лагерь как точка продолжения, ночёвка между забегами | подъём играется урывками, вдвоём — с той же точки | M |
| | `Core/Elbrus/Camp.cs` (правила размещения и ночёвки), `Core/Elbrus/SaveGame.cs` + `Core/Json.cs` + `Core/Elbrus/SaveStore.cs` (схема 1, атомарная запись, слоты по локации и дате), рантайм — `NightSession.Camp.cs`, `Camps.cs`, `CampView.cs`, `Saves.cs`. Палатка-двойка и горелка — предметы стартового набора Эльбруса. «Продолжить» в меню с местом и временем. Сейв у хоста, состояние по игроку (ключ — имя из меню или устойчивый id установки). Акклиматизация растёт по climb high sleep low. Клавиши B/7 и P/8 плюс кнопки в окне рюкзака. Тесты — `CampTests` (22 проверки). Подробности в docs/ELBRUS.md. | | |
| ☑ EL.9 | Помещения, в которые заходят: база проката и ЭВПСО на Азау, приюты и бочки изнутри | внутрь можно войти, на нарах ночуют | M |
| | `Editor/World/ElbrusLodge.cs`: `Elb_Base_Azau` на поляне (прокат со стеллажами и ценником, стол ЭВПСО с журналом, рацией, стендом и картой района, доска погоды `Forecast.BoardText` с термометром и анероидом, лавки, сушилка, чай, объявления, фотографии) и пять приютов, открытых изнутри: бочки (6 мест), малый приют (комната на 4), капсула LeapRus (6), «Дизель-хат» (8 на первом этаже) и новый `Elb_Hut_Natspark`; плюс `Elb_Genset` между бочками. Вход — как у кафе: проём в стене кусками, пол поднят над пивотом, коллайдеры кусками. Маркеры `Elb_RentStand_Desk`, `Elb_RescueDesk`, `Elb_WeatherBoard` и `Elb_Bunk` **все подключены к рантайму** (EL.10–EL.12). 17 тыс. треугольников на семь префабов. Подробности в docs/ELBRUS.md. | | |
| ◐ EL.6 | Люди: очередь на посадку, гиды, группы на косой полке | | L |
| | `Runtime/Parties.cs`: до шести групп выходят из Гара-Баши между 02:00 и 04:00, идут по `Elbrus.SummitRoute` своим темпом с разбросом, разворачиваются по своему контрольному времени и по `DayWeather.BreakHour`, спускаются в 1,6 раза быстрее. Ночью — нитка налобных фонарей постоянного углового размера (видно за полтора километра), днём — точки на фирне. Ни ИИ, ни сети: положение — чистая функция (зерно, день, час). Осталось: очередь на посадку и гиды у станций. | | |
| ☑ EL.10 | Регистрация в ЭВПСО, SOS и спасательная операция | без регистрации никто не ищет; SOS выше 5250 м не уходит | M |
| | `Runtime/RescueDesk.cs` (окно у `Elb_RescueDesk`, кнопка и клавиша SOS) + `NightSession.Rescue.cs` (хост ведёт `Registration`, `Sos`, `Rescue.Mission`, отдаёт владельцу `RescueNet`). Слип лежит в сейве (`SaveClimber.Reg`). Борт — час сбора и двадцать минут подъёма, и только по ясному; пешком от скал Пастухова — часы; без авиации — сутки. | | |
| ☑ EL.11 | Одно зерно на сохранение, погода дня и доска прогноза | доска и погода — из одного `Forecast.Day(seed, date)` | M |
| | `Runtime/MountainDay.cs` (два целых числа по сети — `WeatherSeed` и `WeatherDay`, всё остальное выводится обеими сторонами) + `WeatherBoards.cs` (доска `Elb_WeatherBoard`). Почасовой бросок `WeatherRiskPerHour` заменён на `DayWeather.BreakHour`. Ночёвка двигает дату на сутки. `WeatherBoard.Missed` игроку не показывается. | | |
| ☑ EL.12 | Ночёвка в приюте: койка, цена, сушилка, сохранение | ночь в бочке лучше палатки и так же сохраняет игру | M |
| | `Runtime/LodgeService.cs` (нары `Elb_Bunk`, окно с ценой) + `NightSession.Lodge.cs`: `Lodging.Book` → `Sleep` → `Warm` → `Dry`, запись сейва общая с палаткой (`NightSession.SaveNight`, флаг `camp.tent`). Акклиматизация не дублируется — её считает `Camp.Sleep` внутри `Lodging.Sleep`. | | |
| ☑ EL.13 | Ратрак как услуга: две высадки, цена, отказ по погоде | 4800 и 5100, разворот на 4650, заброска без акклиматизации | M |
| | `Runtime/RatrakService.cs` + `RatrakRide.Go(s)`. Цена рисуется из зерна сохранения. Хост морозит `Climber.HighestEle` на высоте высадки, так что купленные метры в акклиматизацию не идут; `AscentRoute.ColdShock` он уже платил. | | |
| ☑ EL.18 | Программа восхождения: что делать сейчас и зачем | девять дней, двадцать два шага, порядок можно нарушить | M |
| | `Core/Elbrus/Programme.cs` + `Core/Elbrus/ProgrammeDays.cs`: настоящая схема акклиматизации с юга, разложенная на шаги с целью в мире и чистым условием. Ничего не запирает: шаг засчитывается сам, карточка перескакивает вперёд. Прогресс в сейве (схема 2, старые файлы читаются). Доведена до игрока пятью способами: карточка в HUD, стрелка азимута на компасе, отметки на мини-карте и на большой карте, лист в рюкзаке (U/F3) и сообщение о зачёте шага; плюс вступление на первые полминуты на Азау. Хост тикает (`NightSession.Plan.cs`), клиент читает (`Runtime/Programmes.cs`). Подробности в docs/ELBRUS.md. | | |
| ☑ EL.14 | Спуск: направление шага и уход с маршрута | вниз с седловины можно промахнуться мимо коридора скал | M |
| ☑ EL.15 | Канатка: кабина висит отвесно, просека под линией, пассажир не бежит на месте | по докладу Дона после поездки | S |
| | Три штуки из одной поездки: кабина наследовала наклон каната и ехала «приклеенной» к нему — теперь от каната берётся только рыскание, а кузов держит горизонт, как и положено висящему на зажиме; под каждой из шести линий вырублена просека 11 м в полуширину (`ElbrusImporter.UnderTheRopeway`) — внизу первый пролёт от Азау шёл сквозь кроны сосен; и `HikerAnimator` больше не меряет скорость по смещению, когда игрок в кабине, — раньше он «бежал» все шесть метров в секунду, которые за него ехала машина. | | |
| ☑ EL.19 | Игрок застревал в постройках и не мог сойти с места | по докладу Дона после прогулки по Азау | S |
| | Трение у капсулы нулевое (иначе она сползает по склону), поэтому любой порожек новых построек держал её намертво: раньше выручало то самое сползание. `HikerController.StepOver` поднимает игрока на препятствие до 45 см, если над ним чисто и наверху можно стоять, а `Wedged` ловит случай «жму, а стою» (0,45 с и меньше 0,3 м) и выталкивает вверх и назад. Заодно телепорт по маршруту (F4) перестал засчитываться как пройденные метры: кубик срыва и трещин бросается на каждый метр, и триста метров разом — это гарантированное падение (`NightSession.Ascent.Walkable`). |  |  |
| ◐ EL.16 | Деньги: в игре нет ни одного источника дохода | кошелёк 7000 ₽ на всё: прокат 3900, ратрак 5000–11000, койка 2000–6000 | M |
| ☑ EL.17 | Ночёвка двигает дату и переводит часы на утро | лёг в 23:30 — вставал в 23:30 следующего дня | S |
| | `NightRun.NewMorning()` (отличается от `SkipAhead` только направлением) возвращает часы к `AscentRoute.RunStartHour`, сейв пишется с нулевым `elapsed`. Только для засчитанной ночи, так что дату и утро нельзя прокрутить кнопкой. |  |  |
| | `NightSession.Ascent.cs` заполняет `Ascent.Step.Way/Hour/CatTrack/Tiredness`; `Report.WanderPerMetre` хост умножает на разыгранную сторону (держит 25 с) и кладёт в `ClimbNet.Wander`, владелец сдвигает себя поперёк линии в `ClimbGear.Drift`. `RouteLost` и `MissedTheGate` — в HUD. | | |

## 4б. Производительность и устойчивость (аудит 19.09.2026)

Аудит по коду плюс замеры мак-сборки: **Apple M5, полноэкранный рендер 3420×2146 (7,3 Мпикс)**. Сборка 416 МБ,
`Assets/Resources/World` — 452 МБ в 3410 ассетах. Порядок в таблицах — по отдаче, а не по сложности; после первой
волны он оказался неверным, см. замеры ниже.

### Производительность

| | Что | Сейчас в коде | Размер |
|---|---|---|---|
| ☑ PF.1 | Нет ни одной ручки качества: разрешение, vSync, тени, каскады | `Bootstrap.cs:46–49` ставит `targetFrameRate = 120` при включённом vSync (не работает) и только **поднимает** качество через `Mathf.Max`; `Screen.SetResolution` и `vSyncCount` не встречаются нигде, `ProjectSettings/QualitySettings.asset` в репозитории нет | M |
| | Пока игра всегда рисует в нативном ретина-разрешении: на маке, где замеряли, это 7,3 Мпикс на кадр. Рендер-скейл 0,75 убирает 44 % пикселей, 0,7 — половину, и на сцене, упирающейся в заливку (трава, сплат-шейдер), это даёт больше, чем все остальные пункты вместе. Нужен экран настроек: разрешение/скейл, дальность теней и каскады, дальность травы и деревьев, vSync. Заодно зафиксировать уровень качества в `ProjectSetup`, а не полагаться на дефолт машины. | | |
| ☑ PF.2 | Трава на поляне Азау: дальность 95 м при плотности 1 | `TerrainBuilder.cs:39–40`; пики `ElbrusImporter.cs:287` (84/40/22 на ячейку 6 м), `DetailPerPatch = 8` при `DetailRes = 2048` — 65 536 патчей деталей | S |
| | В круге 95 м это 40–115 тыс. билбордов на двухпроходном альфа-шейдере с вершинной анимацией — ровно там, где и падает fps. Дальность 55–65 и плотность 0,6–0,7 правятся в рантайме без пересборки мира; `DetailPerPatch = 16` требует `PipelineVersion +1`. | | |
| ☑ PF.3 | `basemapDistance = 2200` при `baseMapResolution = 1024` | `TerrainBuilder.cs:33`, `ElbrusImporter.cs:72` | S |
| | До 2,2 км земля рисуется полным четырёхслойным сплатом (≈9 выборок на пиксель). Дефолт Unity — 1000. Радиус ставили большим потому, что базовая карта на 12,3 км даёт 12 м на тексель и всё мылится: правильная пара — 600–700 м дальности и `baseMapResolution = 2048`. | | |
| ◐ PF.4 | Террейн кидает тень как `TwoSided` и рисуется без инстансинга | `TerrainBuilder.cs` | S |
| | Тень сделана (`ShadowCastingMode.On`). **Инстансинг откатан**: в редакторе он работает, а в сборке инстансного варианта `Nature/Terrain/Standard` нет — на него никто не ссылается, и Unity его вырезает (ловушка из CLAUDE.md §4). Террейн в плеере нарисовался вообще без текстур: белая равнина с деревьями. Включать обратно — только вместе с тем, чтобы затащить вариант в сборку (материал в `Resources` или Always Included в `ProjectSetup`) и проверить **в сборке**. 4096 патчей хайтмапа — это 4096 вызовов отрисовки, и по замерам ниже они как раз в числе главных. | | |
| ☑ PF.5 | Застройка Азау: ~700 рендереров, ни статического батчинга, ни LOD | `ElbrusWorld.cs:130`, `:210`, фабрика частей `ElbrusProps.cs:24` (кафе — 32 рендерера, база — 46) | M |
| | Ни одного `StaticBatchingUtility.Combine` и ни одной `LODGroup` на постройках; `isStatic` не ставится. Батчить группами (Азау, каждая станция), киоскам и машинам дать отсечение по высоте на экране. Канатки из батча исключить — они двигаются. | | |
| ☑ PF.6 | HUD переписывается целиком каждый кадр | `HudController.cs:786` (около 20 интерполированных строк, два `new StringBuilder`, отладочная строка с 15 подстановками и двумя `TerrainBuilder.Height`) | S |
| | Меняющийся каждый кадр текст делает Canvas грязным и пересобирает батч всего HUD. Часы, партию и подсказки обновлять раз в 0,2 с или по изменению значения, отладочную строку — раз в 0,25 с и на отдельном Canvas. | | |
| ☑ PF.7 | Канатки: ~170 кабин двигаются каждый кадр независимо от расстояния | `RopewayRig.cs:153` | S |
| | С седловины все шесть линий продолжают писать позиции и повороты ~880 рендерерам без LOD. Двигать только линии ближе N км, дальние — на 10 Гц, кабины гасить по расстоянию. | | |
| ◐ PF.8 | Шесть физических запросов на `FixedUpdate` с маской `~0` | `HikerController.cs:312`, `:316`, `:487–490` (`StepOver`), `:511`, `:519`, `:647` | S |
| | `~0` означает проверку против всех коллайдеров, включая ~52 тыс. стволов и ~39 тыс. валунов. Завести маски `Ground|Props` и `LowSpace`; `StepOver` пускать только когда игрок реально упёрся (горизонтальная скорость меньше желаемой). | | |
| ☑ PF.9 | `Bootstrap.LocalHiker` делает `GetComponent` при каждом обращении | `Bootstrap.cs:29` | S |
| | Свойство читают из `Update`/`LateUpdate` полтора десятка систем, HUD — несколько раз за кадр. Кэшировать в поле, обновляя в `OnClientConnected`/`OnNetworkSpawn`/`OnNetworkDespawn`. Туда же `NightSession.Token` (`NightSession.cs:84`) — строка `"c" + id` рождается 6–8 раз на игрока в кадр. | | |
| ☑ PF.10 | Линейные проходы по полилинии маршрута в каждом кадре | `Elbrus.cs:220` (`PointAt`, 49 сегментов с `sqrt`), вызовы: `Parties.cs:158–171` каждый кадр, `ClimbGear.cs:132` на 50 Гц | S |
| | Чужие группы на склоне — чистая декорация, а стоят порядка 2000 `sqrt` в кадр. Считать раз в 0,2 с и интерполировать; у владельца кэшировать `(s, offset)` и пересчитывать при смещении больше 2 м. | | |
| ☑ PF.11 | Шесть сервисов каждый кадр собирают строку подсказки | `CafeService.cs:365`, `LodgeService.cs:328`, `RentalService.cs:382`, `RescueDesk.cs:299`, `WeatherBoards.cs:198`, `ElbrusRides.cs:58` | S |
| | Проверено по каждому: кафе, ночлег, прокат и спасотряд возвращают `""` сразу, если рядом никого нет, — строка рождается только у стойки. Осталась одна настоящая: подсказка канатки пересобиралась из бегущих чисел (метры, высота, секунды до кабины) все двадцать минут подъёма — теперь 10 Гц (`ElbrusRides.PromptStep`), очистка по-прежнему мгновенная. `Backpacks.WorkHere` кэшируется на кадр. | | |
| ☑ PF.12 | Сеть: постоянный трафик на неизменных данных | ростер партии в `PersonalRpc` (`NightSession.cs:187`) — `FixedString512` каждому 4 Гц; `MenkPos`/`MenkYaw` (`:134`) — каждый кадр, и `TickMenk` крутится даже на Эльбрусе; `TorchLevel` (`Equipment.cs:94`) — каждый кадр при «жучке»; три `NetworkList` целиком при любом изменении рюкзака (`NightSession.Packs.cs:139`) | M |
| | Ростер уехал в `PartyRpc` и шлётся только когда строка изменилась; `PersonalRpc` его больше не возит. `TickMenk` выходит первой строкой, если Менка нет (Эльбрус — всегда). `MenkPos`/`Yaw`/`State`/`Blows` пишутся по изменению. «Жучок» квантован до 16 ступеней. Диффы рюкзаков — третьей волной. | | |
| ◐ PF.13 | 452 МБ в `Resources/World`, 3410 ассетов; DEM разбирается побайтово каждую сессию и при каждой смене места | `Dem.cs:37–44` (цикл по 4,2 млн элементов), `TerrainBuilder.cs:14–19`; `Meshes/Trees/*_LOD0.asset` по 3 МБ | M |
| | На fps не влияет, влияет на старт, память и вес сборки. R16 декодируется через `Buffer.BlockCopy` (со сверкой порядка байтов) вместо цикла по 4,2 млн элементов. Кэш `HeightField` по месту и сборка мира корутиной — третьей волной. | | |

### Устойчивость

| | Что | Чем кончается | Размер |
|---|---|---|---|
| ☑ ST.1 | `LeaveRide` выходит, если сиденья уже нет | `HikerController.cs:556`: `if (!IsOwner \|\| Ride == null) return;` — кабина уничтожена, значит `Riding` остаётся `true`, тело кинематическое, капсула выключена. Хост перестаёт бросать кости, ворота и холод проходят мимо: игрок висит в воздухе и неуязвим. Сбрасывать состояние безусловно, `Ride == null` проверять только ради `Place(pos)` | S |
| ☑ ST.2 | Вторая «ночёвка» в том же лагере пишет сейв с вечерним временем | `NightSession.Camp.cs:324`: `firstNight = false` → блок нового утра пропускается → в файл уходит текущий `Elapsed`. Загрузка даёт несколько минут до рассвета, и пять таких нажатий вытесняют хороший утренний сейв (`SaveStore.Prune`, 5 слотов). Либо ставить `save.Elapsed = 0` всегда, либо отказывать и говорить «здесь вы уже ночевали» | S |
| ☑ ST.3 | Сейв под ключом `c1` вместо имени игрока | `NightSession.Camp.cs:394`: профиль создаётся с ключом-токеном, настоящий ключ приходит отдельным RPC кадром-двумя позже (`ClimbGear.cs:121`). Ночёвка в этом окне теряет при загрузке акклиматизацию, рюкзак, программу, талон ЭВПСО и деньги, а сирота копируется во все следующие файлы | S |
| ☑ ST.4 | Числа из сейва не проверяются на конечность | `SaveGame.cs:220`, `:230–232`: `1e999` парсится в `Infinity`, `Math.Max` его пропускает, координат лагеря не проверяет никто. Дальше NaN в `Rigidbody`, из которого не выйти. Прогонять через `float.IsFinite`, координаты зажимать в `±World.Half` | S |
| ☑ ST.5 | `Json.ParseValue` рекурсивен без ограничения глубины | `Json.cs:229`: файл из десятков тысяч `[` даёт `StackOverflowException`, который в .NET не ловится ничем. А `SaveStore.List` читает все файлы папки при каждом нажатии кнопки места в меню — игра не стартует, пока файл не удалят руками. Счётчик глубины и `FormatException` после 64 | S |
| ☑ ST.6 | Протокол ночи не ограничен ничем | `NightSession.cs:96`: позднему гостю уходит по одному reliable-RPC на каждое событие в одном кадре (сотни сообщений по ~0,5 КБ переполняют очередь UnityTransport), а `HudController.cs:844` пересобирает всю строку целиком и упирается в лимит 65 535 вершин uGUI. Хранить и слать последние ~300 | S |
| ☑ ST.7 | Имя игрока не чистится | `HikerController.cs:138`: `FixedString64Bytes` бросает на имени длиннее 61 байта (поле меню считает символы), и бросок происходит **до** создания камеры — дальше NRE каждый кадр. `NightSession.cs:203`: `;` или `\|` в имени ломают разбор пакета партии, `int.Parse` падает 4 раза в секунду. Обрезать по байтам и заменять разделители | S |
| ☑ ST.8 | `Loose` растёт без потолка | `Core/Packs.cs:309`: брошенные предметы и нарубленные ветки не удаляются никогда, а любое изменение рюкзака гонит весь список по сети. Потолок с вытеснением самого дальнего, как в `SkiTrailFx` | S |
| ☑ ST.9 | `SetPlace` сносит мир до загрузки DEM | `Bootstrap.cs:72–84`: `World.Current` меняется и старый мир уничтожается, а `LoadDem` может бросить `FileNotFoundException` — остаётся новая локация со старой сеткой высот. Грузить DEM во временную переменную первой строкой | S |
| ☑ ST.10 | Запись сейва не атомарна | `SaveStore.cs:79`: `File.Delete` + `File.Move` вместо `File.Move(tmp, path, true)` — окно, в котором файла нет вовсе, хотя комментарий обещает атомарность | S |
| ☑ ST.11 | Состояние по клиенту чистится только через `PruneClimb` | `NightSession.Ascent.cs:646`: охрана `climbers.Count <= ConnectedClients.Count` пропускает случай «один ушёл, другой пришёл в том же тике», а `profiles`/`watches`/`forecastSeen` наполняются из RPC и в `OnClientDisconnected` не трогаются вовсе. Чистить из `OnClientDisconnected`, `PruneClimb` оставить страховкой | S |
| ☑ ST.12 | Клиент считает воздух не по тем числам, что хост | `ClimbGear.cs:134` берёт `freshSnowCm = 0` и локальные `Weather.Storm`/`Wind` со своей фазой порывов, хост — накопленный снег и `StormNow`. HUD расходится с костями, которые решают срыв. В `ClimbNet` уже едут `WindMs` и `Feels` — читать их; `FreshSnow` либо читать, либо удалить | S |
| ☑ ST.13 | `Saves` глушит любое исключение | `Saves.cs:60`: `catch (Exception)` ловит и полный диск, и регрессию схемы — игрок просто играет без сохранений и не знает об этом. Ловить `IOException`/`UnauthorizedAccessException`/`FormatException`, остальное в `LogError` | S |
| ☑ ST.14 | Сейв нечем покрыть тестом | `CampTests.cs:335` проверяет обрезанный и чужой файл, но не числа вне диапазона и не вложенность, а правило «что кладётся в сейв» живёт в `partial` от `NetworkBehaviour` и не собирается ни в dotnet, ни в EditMode. Вынести `SaveGame BuildSave(...)` чистой функцией в Core и покрыть тремя тестами: ночь спали, ночь не спали, загрузка обратно | M |
| | Правило переехало в `Core/Elbrus/CampSave.cs`: рантайм собирает (только он знает про клиентов, рюкзаки и рельеф), ядро решает. Четыре теста (`CampSaveTests`): ночь спали — часы к утру, день +1, снег обнулён; не спали — не двигается ничего; безымянный (`c1`) не пишется вовсе; вчерашний напарник остаётся в файле и переживает круг через диск. Живое состояние сессии теперь выставляется **из** сейва, а не считается рядом с ним — дата, часы и снег не могут разойтись с тем, что записано. | | |
| ☑ ST.15 | `BoxCollider does not support negative scale` в логе плеера | `SiteFactory.cs:54`, объект `Site_Tent_1959`: отрицательный масштаб на коллайдере, геометрия столкновений непредсказуема | S |
| ☐ ST.16 | NaN в границах двух мешей трещин | Сборка 19.09.2026 отчитывается «Succeeded, 414 MB, **4 errors**», и все четыре — одно и то же: `Mesh 'Asc_Crevasse_3_11'` и `Asc_Crevasse_2_10` — `abnormal mesh bounds … min=(nan, nan, nan), max=(nan, nan, nan)`. Меш с NaN-границами не отсекается по пирамиде видимости предсказуемо: он либо рисуется всегда, либо пропадает ни с того ни с сего. Генерит `ElbrusAscent` (трещины на леднике). Найти вершину, из которой приходит NaN, — скорее всего деление на нулевую длину при построении края трещины | S |

**Сделаны обе волны** (19.09.2026, `PipelineVersion 52`). Мерить можно прямо в игре: **F10**, синхронизацию
выключить, смотреть миллисекунды. Поляна Азау, полный экран 3420×2146, высокое качество:

| Куда смотрим | Было | Стало |
|---|---|---|
| От посёлка в гору | 20,0 мс (50 fps) | **9,2 мс (107 fps)** |
| Лицом на посёлок от точки старта | 34,2 мс (29 fps) | **7,2 мс (138 fps)** |
| Через всю площадь вниз по долине (худший вид) | — | 18,5 мс (54 fps); на низком 11,2 |
| Вершинное плато 5632 м | — | 7,9 мс (126 fps) |

Что оказалось правдой, а что нет:

1. **Первая волна — пиксели, трава, базовая карта — почти ничего не дала.** Втрое меньше пикселей давали те же
   19 мс, низкое качество вместо высокого — те же 20. Сцена не упиралась ни в заливку, ни в дистанции.
2. **Всё решали вызовы отрисовки.** Две с половиной сотни префабов по тридцать–сорок отдельных `MeshRenderer` — это
   полторы тысячи вызовов со сменой материала почти на каждом. `StaticBatchingUtility.Combine` по группам «Азау» и
   «Станции» плюс слой мелочи с отсечением на 260 м (`Runtime/Scenery.cs`) сняли разницу между «смотрю на посёлок»
   и «смотрю от посёлка» целиком: 34,2 → 7,2 мс.
3. **Остальное — процессорная мелочь, стоившая ещё вдвое**: HUD переписывался весь каждый кадр, 170 кабин канаток
   двигались даже с седловины, чужие группы считали около двух тысяч корней в кадр, `WorkHere` считался трижды за
   кадр, токен клиента рождался строкой по восемь раз на игрока. Отсюда 20 → 9,2 мс на обычном виде.
4. **Что осталось дорогим**: вид через всю площадь вниз по долине — 18,5 мс на высоком против 11,2 на низком.
   Разница живёт в дальности теней (140 м против 45) и в траве, и это уже вопрос вкуса, а не поломки: на то и ручка.

**Третья волна** (19.09.2026) добила устойчивость и сеть: ST.3, ST.8, ST.11, ST.12, ST.13, ST.14, PF.11, PF.12 и
декодирование рельефа из PF.13. Кадр это почти не двигает — здесь чинили не мерцание цифры, а то, что портится
молча: сейв под ключом `c1`, список лежащих вещей без потолка, состояние ушедшего клиента, разошедшиеся числа
воздуха у клиента и хоста, проглоченное исключение записи.

Осталось в бэклоге: **PF.4** (инстансинг террейна — сначала затащить вариант шейдера в сборку), **PF.8** (маски
физики; `StepOver` уже не стреляет, пока игрок реально не упёрся), остаток **PF.13** (452 МБ в `Resources`,
кэш `HeightField`) и третья волна по крупному: диффы рюкзаков, кэш точки маршрута, загрузка мира корутиной.

Две вещи, которые стоит помнить при замерах. Панель на этой машине 144 Гц, и с включённой синхронизацией кадр либо
укладывается в 6,9 мс, либо падает до 72 fps — мерить надо миллисекунды и только с выключенной синхронизацией. И
второе: **цифры, снятые на сломанном рендере, ничего не значат** — первые замеры этой волны были сняты с
инстансингом, то есть с террейном без текстур, и показывали 13,9 мс вместо настоящих 20.

## 5. Вехи

**M1 — «Два глагола» (цель: понять, ощущается ли).** E0.1–0.4, E1.1–1.5, E3.1–3.2, E7.1–7.2, E7.5. Сцена: четверо от лагеря до лабаза и обратно по целине, поставить палатку. Вопрос плейтеста один: хочется ли меняться местами и тянуть оттяжку вдвоём, или это раздражает.

**M2 — «Ночёвка».** E3.3–3.7, E4.1–4.4, E6.1–6.2. Полная ночёвка с печкой, сушкой, дежурством.

**M3 — «Дорога».** E2.x, E5.x, E1.6, E7.3. Переход с графиком и лабазом (это же миссии 7–8 пролога).

**M4 — «Угрозы через дежурного».** E6.3–6.4 + балансировка.

Оценка M1 при текущем темпе — 3–4 недели чистой работы; главные риски — физическая верёвка по сети (7.1) и след на террейне (1.3). Обе можно временно упростить: верёвка — как spring joint между двумя точками без промежуточных звеньев; след — как список сегментов, по которым считается стоимость, без визуала.

## 6. Что не делаем

- Не копируем PEAK-предметы (леденцы, воздушные шары) и врагов. Наши предметы — только то, что было в рюкзаках группы.
- Никаких «знаков», огней, намёков на версии — правило пролога действует и на угрозы дежурства.
- Не вводим второй бар. Всё — куски одной полоски.

## Источники по PEAK

- https://peak.wiki.gg/wiki/Status_effects — стамина 20/с, тики 2,5, травма без регенерации, холод и кучкование 1,5/с в 3,2 м.
- https://peak.wiki.gg/wiki/Campfire — все в 19,2 м, +25 стамины, −20 травмы, таймер 60 с, туман и автосейв.
- https://peak.wiki.gg/wiki/Peak_(game) — биомы, туман на высоких сложностях, ротация карты.
- https://www.gamesradar.com/games/co-op/peak-devs-originally-borrowed-from-zelda-breath-of-the-wilds-climbing-but-everything-changed-1-week-into-development-at-this-point-the-game-kind-of-made-itself/ — как один глагол «сделал игру сам».
- https://discussions.unity.com/t/how-does-character-controller-work-in-peak-game/1683399 — разбор контроллера: две руки, две ноги, пассивное тело на пружинах, активного рэгдолла нет.
